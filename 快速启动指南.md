# 🚀 A2A 协议示例项目 - 快速启动指南

## 📖 项目简介

**Agent-to-Agent (A2A)** 是 Google 推出的智能体间通信协议，让不同框架、不同语言的 AI Agent 能够无缝协作。

### 核心概念

- **Agent（智能体）**: 运行 A2A 服务器的 AI 助手，提供特定能力
- **Host（宿主）**: 运行 A2A 客户端的应用，协调多个 Agent
- **AgentCard（智能体卡片）**: Agent 的"名片"，描述其能力和接口
- **Task（任务）**: Agent 之间传递的工作单元

---

## 🎯 三种启动方式

### **方式一：Demo Web 应用（最推荐）**

可视化界面，支持多 Agent 协作，最适合初学者体验。

#### 步骤 1: 准备 API Key

您需要以下任一方式的认证：

**选项 A: Google AI Studio API Key（推荐）**
1. 访问 [Google AI Studio](https://aistudio.google.com/app/apikey)
2. 创建或获取 API Key
3. 保存备用

**选项 B: Google Cloud Vertex AI**
1. 安装并配置 gcloud CLI
2. 运行 `gcloud auth login` 认证
3. 准备好项目 ID 和区域

#### 步骤 2: 启动 Demo 应用

```bash
# 使用快速启动脚本（推荐）
./quick_start.sh
# 然后选择选项 1

# 或者手动启动
cd demo/ui

# 创建 .env 文件
echo "GOOGLE_API_KEY=你的API密钥" > .env

# 启动应用
uv run main.py
```

#### 步骤 3: 访问 Web 界面

打开浏览器访问：**http://localhost:12000**

#### 步骤 4: 添加 Agent

在另一个终端启动示例 Agent：

```bash
cd samples/python/agents/adk_expense_reimbursement
cp ../../demo/ui/.env ./
uv run .
```

在 Web 界面中：
1. 点击 🤖 机器人图标
2. 输入 Agent 地址：`localhost:10002`
3. 开始对话！

#### 体验建议

尝试这些对话：
- "What remote agents do you have access to?"（查看可用的 Agent）
- "Reimburse lunch for 20 EUR, convert to USD first"（测试费用报销）
- "What can you do?"（了解 Agent 能力）

---

### **方式二：Python CLI + Agent（开发者推荐）**

命令行方式，适合开发调试。

#### 步骤 1: 启动 Agent 服务器

```bash
cd samples/python/agents/langgraph

# 配置 API Key
echo "GOOGLE_API_KEY=你的API密钥" > .env

# 启动服务器（默认端口 10001）
uv run .
```

#### 步骤 2: 在新终端启动客户端

```bash
cd samples/python/hosts/cli

# 启动 CLI 客户端
uv run .
```

#### 步骤 3: 测试对话

```
You: Convert 100 USD to EUR
Agent: [执行货币转换...]
```

---

### **方式三：Java Agent（企业级）**

基于 Quarkus + LangChain4j 的 Java 实现。

#### 前置条件
- Java 17 或更高版本
- Maven 3.8+

#### 启动天气 Agent

```bash
cd samples/java/agents/weather_mcp

# 配置 OpenWeatherMap API Key
echo "OPENWEATHER_API_KEY=你的API密钥" > .env

# 启动服务（开发模式）
./mvnw clean quarkus:dev
```

服务启动后访问：**http://localhost:10003**

---

## 🏗️ 项目架构

```
┌─────────────────────────────────────────────────┐
│              用户界面层                          │
│  Web UI (Mesop) / CLI / 其他客户端               │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│            Host Agent（宿主智能体）              │
│  - 接收用户请求                                  │
│  - 协调多个 Remote Agent                         │
│  - 聚合返回结果                                  │
└──────────────────┬──────────────────────────────┘
                   │
         ┌─────────┴─────────┐
         ▼                   ▼
┌──────────────────┐  ┌──────────────────┐
│  Remote Agent 1  │  │  Remote Agent 2  │
│  (费用报销)       │  │  (货币转换)       │
└──────────────────┘  └──────────────────┘
```

---

## 📚 示例 Agent 列表

### Python Agent

| Agent 名称 | 功能 | 框架 | 端口 |
|-----------|------|------|------|
| adk_expense_reimbursement | 费用报销 | Google ADK | 10002 |
| langgraph | 货币转换 | LangGraph | 10001 |
| crewai | 图像生成 | CrewAI | 10007 |
| llama_index_file_chat | 文件分析 | LlamaIndex | 10010 |
| adk_facts | 趣味事实 | Google ADK | 10000 |

### Java Agent

| Agent 名称 | 功能 | 框架 | 端口 |
|-----------|------|------|------|
| content_writer | 内容创作 | LangChain4j | 10020 |
| content_editor | 内容编辑 | LangChain4j | 10021 |
| weather_mcp | 天气查询 | LangChain4j | 10003 |

---

## 🛠️ 常见问题

### Q1: UV 是什么？如何安装？

UV 是 Python 的现代包管理工具，比 pip 更快。

```bash
# macOS/Linux
curl -LsSf https://astral.sh/uv/install.sh | sh

# 或使用 Homebrew
brew install uv
```

### Q2: 如何查看 Agent 之间的通信？

在 Demo Web 应用中：
1. 点击"Events"标签查看消息流
2. 点击"Tasks"标签查看任务状态

### Q3: 如何创建自己的 Agent？

参考 `samples/python/agents/` 目录下的示例：

```python
# 最简示例
from a2a_python import A2AServer

server = A2AServer(
    name="My Agent",
    description="我的第一个 Agent",
    port=10050
)

@server.task_handler
async def handle_task(task):
    # 处理任务逻辑
    return {"result": "完成！"}

server.run()
```

### Q4: 多个 Agent 如何协作？

Host Agent 会：
1. 接收用户请求
2. 根据 AgentCard 的能力描述选择合适的 Remote Agent
3. 将任务分发给选中的 Agent
4. 聚合结果返回给用户

### Q5: 支持哪些传输协议？

- HTTP/REST（默认）
- gRPC
- WebSocket
- Server-Sent Events (SSE)

---

## 🔐 安全注意事项

⚠️ **重要提示**：这是示例代码，不适合直接用于生产环境！

生产环境必须：
1. ✅ 验证和清理所有来自外部 Agent 的输入
2. ✅ 实现身份认证和授权机制
3. ✅ 使用 HTTPS 加密通信
4. ✅ 实施速率限制和资源配额
5. ✅ 监控和日志记录所有交互

参考 `samples/java/agents/magic_8_ball_security` 了解安全实现。

---

## 📖 延伸学习

- **A2A 协议规范**: https://goo.gle/a2a
- **Python SDK**: https://github.com/google/a2a-python
- **Inspector 工具**: https://github.com/a2aproject/a2a-inspector
- **ADK 文档**: https://google.github.io/adk-docs/

---

## 🎉 下一步

1. ✅ 启动 Demo 应用体验多 Agent 协作
2. ✅ 查看 `samples/` 目录学习不同框架的实现
3. ✅ 修改示例代码创建自己的 Agent
4. ✅ 探索 `extensions/` 目录了解协议扩展
5. ✅ 查看 `notebooks/` 学习评估和部署

**祝您探索愉快！🚀**
